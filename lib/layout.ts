/** Shared page width — centered, slight left nudge (half of prior offset) */
export const pageContainer =
  "max-w-7xl mx-auto w-full box-border px-6 md:px-12 -translate-x-1 md:-translate-x-1.5 lg:-translate-x-2";

/** Centered page hero — Categories, Blogs, Resources, About */
export const pageShell = "min-h-screen pt-14 md:pt-20 pb-12 w-full";

export const pageHeaderBlock = "w-full text-center mb-10 md:mb-12";

export const pageTitle =
  "text-[clamp(28px,4vw,42px)] font-extrabold text-foreground mb-3";

export const pageSubtitle =
  "text-base md:text-lg text-muted-foreground max-w-2xl mx-auto";

export const pageSearchRow = "flex justify-center w-full mb-8 md:mb-10";

export const pageControlsRow =
  "flex flex-col items-center justify-center gap-5 w-full mb-10 md:mb-12";

/** Filter pills left, sort control right */
export const pageFiltersRow =
  "flex flex-col md:flex-row md:items-center justify-between gap-4 w-full mb-8 md:mb-10";
